apiVersion: v1
kind: Service
metadata:
  name: learning
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.global.serviceType }}
  selector:
    app: learning
  ports:
    - port: 8080
      targetPort: 8080
      {{- if eq .Values.global.serviceType "NodePort" }}
      nodePort: {{ .Values.learning.nodePort }}
      {{- end }}
