apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: learning-backend-pdb
  namespace: {{ .Values.global.namespace }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: learning-backend
