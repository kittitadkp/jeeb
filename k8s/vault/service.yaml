apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: jeeb
spec:
  selector:
    app: vault
  type: NodePort
  ports:
    - name: api
      port: 8200
      targetPort: 8200
      nodePort: 30091
    - name: cluster
      port: 8201
      targetPort: 8201
